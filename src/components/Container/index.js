//Container Component
import React from "react";


const Container = () => (

    <div className="container-fluid bg-info">
   <h3 className = "text-center">Click on an image to earn points, but don't click the same image twice.</h3>
  </div>
);

export default Container;